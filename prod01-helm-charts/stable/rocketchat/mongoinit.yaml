version: v1
kind: ConfigMap
metadata:
  name: mongo-init
data:
  init-mongo.js: |
    use admin
    db.createUser(
      {
        user: “rocketchat”,
        pwd: “changeme”,
        roles: [
          {
            role: "readWrite",
            db: “rocketchat”
          }
        ]
      )
